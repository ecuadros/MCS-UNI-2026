#ifndef UTIL_H
#define UTIL_H

// Función original de la clase (Ernesto)
// Intercambia los valores de dos variables enteras
void intercambiar(int &a, int &b);

// ---------------------------------------------------------
// TAREA ALUMNO 12 (Julián): Función Redondear
// Recibe un número decimal (double) y devuelve el entero más cercano
// ---------------------------------------------------------
int redondear(double numero);

#endif
